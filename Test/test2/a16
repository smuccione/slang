class test
  local aData
  method loadArray()
        a = {}
        a[1][1] = "hello"
        a[2][1] = "there"
        a[3][1] = "how"
        a[4][1] = "are"
        a[5][1] = "you"
        a[1][2] = 5
        a[2][2] = 3
        a[3][2] = 1
        a[4][2] = 2
        a[5][2] = 4
        return a
  end
  method new()
     ::aData = ::loadArray()

     return 1
  end
  method put()
        local l

        l = len ( ::aData )
        a = ::aData

        println ( "-----------------" )
        for ( loop = 1; loop <= len ( a ); loop++ )
           print ( a[loop][1], " " )
        end
        println()
        for ( loop = 1; loop <= len ( a ); loop++ )
           print ( a[loop][2], " " )
        end
        println()

        asort ( a, 0, 2 )

        println ( "-----------------" )
        for ( loop = 1; loop <= len ( a ); loop++ )
           print ( a[loop][1], " " )
        end
        println()
        for ( loop = 1; loop <= len ( a ); loop++ )
           print ( a[loop][2], " " )
        end
        println()

  end
end

test = new ( "test" )
a = test.put();

a = { {"hello", 5},
	{"there", 3},
	{"how",1},
	{"are",2},
	{"you",4}
     }

println ( "-----------------" )
for ( loop = 1; loop <= len ( a ); loop++ )
   print ( a[loop][1], " " )
end
println()
for ( loop = 1; loop <= len ( a ); loop++ )
   print ( a[loop][2], " " )
end
println()

println ( "-----------------" )
a = asort ( a, 0, 2 )
println ( "-----------------" )
for ( loop = 1; loop <= len ( a ); loop++ )
   print ( a[loop][1], " " )
end
println()
for ( loop = 1; loop <= len ( a ); loop++ )
   print ( a[loop][2], " " )
end
println()

println ( "-----------------" )
a = asort ( a, 1, 2 )
println ( "-----------------" )
for ( loop = 1; loop <= len ( a ); loop++ )
   print ( a[loop][1], " " )
end
println()
for ( loop = 1; loop <= len ( a ); loop++ )
   print ( a[loop][2], " " )
end
println()

a = {}
a[1][1] = "hello"
a[2][1] = "there"
a[3][1] = "how"
a[4][1] = "are"
a[5][1] = "you"
a[1][2] = 5
a[2][2] = 3
a[3][2] = 1
a[4][2] = 2
a[5][2] = 4

println ( "-----------------" )
a = asort ( a, 0, 2 )
println ( "-----------------" )
for ( loop = 1; loop <= len ( a ); loop++ )
   print ( a[loop][1], " " )
end
println()
for ( loop = 1; loop <= len ( a ); loop++ )
   print ( a[loop][2], " " )
end
println()
