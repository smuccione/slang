local a

a = {
		"i",
		"love"
	}

aAdd ( @a, "donna" )

foreach ( it in a )
{
	print ( it, " " )
}

println()

aSize ( @a, 4 )

foreach ( it in a )
{
	print ( it, " " )
}

println()

aSize ( @a, 2 )

foreach ( it in a )
{
	print ( it, " " )
}
println()
